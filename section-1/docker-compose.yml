version: "3.9"

services:
  web:
    build: .            # build Docker image from the Dockerfile in the current directory
    ports:
      - "5000:5000"     # map host port 5000 to container port 5000
    restart: always     # always restart the container if it stops or Docker restarts